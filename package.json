{
  "name": "echo-webhook-relay",
  "version": "1.0.0",
  "description": "ECHO PRIME Webhook Relay v1.0 — Centralized webhook management: register, receive, verify, fan-out, retry, dead-letter queue | HMAC-SHA256 | Exponential backoff | Payload transformation",
  "author": "Echo Prime Technologies <bob@echo-op.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.20",
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.5.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.0.0"
  }
}
